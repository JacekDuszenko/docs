labels:
  critical:
    description: Critical changes
    color: fd5e53

workflows:
  - name: critical
    run:
      if: $hasFileName("main.go")
      then: $addLabel("critical")
  - name: configurations
    run:
      if: $hasFileName("config.yml")
      then: $assignTeamReviewer(["devops"])
