import CodeBlock from "@theme/CodeBlock";
import Example from "!!raw-loader!./example.yml";

**Description**:

Evaluates if a given text begins with a specific sentence, returning true or false accordingly
Note that this evaluation is case-sensitive.

**Parameters**:

| Parameter | Type   | Description            |
| :-------- | :----- | :--------------------- |
| `text`    | string | The text to search in. |
| `prefix`  | string | The prefix.            |

**Return value**:

| Type      | Description                                                  |
| :-------- | :----------------------------------------------------------- |
| `boolean` | `true` if `prefix` is a prefix of `text`, `false` otherwise. |

**Examples**:

```yml
$startsWith("Testing string contains", "Test")                 #true
$startsWith("Testing string contains", "string contains")      #false
```

A `reviewpad.yml` example:

<CodeBlock language="yml" title="reviewpad.yml">{Example}</CodeBlock>
