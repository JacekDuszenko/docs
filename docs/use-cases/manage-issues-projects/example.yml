workflows:
  - name: manage-issues-in-projects
    on:
      - issue
    run:
      - if: '$state() == "open" && !$isLinkedToProject("PROJECT-TITLE")'
        then: $addToProject("PROJECT-TITLE", "Todo")
      - if: $state() == "closed"
        then: $addToProject("PROJECT-TITLE", "Done")
